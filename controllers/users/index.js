const getCurrentUser = require('./getCurrentUser');
const updateSub = require('./updateStatus');
const avatar = require('./avatar');

module.exports = {
    getCurrentUser,
    updateSub,
    avatar
}