const { HttpCode, Role, MaxReqBodySize, FOLDER_CLOUD_AVATAR } = require('./constans');
const AppErrorCode = require('./appErrors');

module.exports = {
    HttpCode,
    Role,
    MaxReqBodySize,
    FOLDER_CLOUD_AVATAR,
    AppErrorCode,
}